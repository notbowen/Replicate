# Replicate

A MacOS menu-bar wrapper around `rsync` and `fswatch` that allows one to keep 2 folders on their Mac in sync.

Vibe-coded this with Codex to sync my photos to a mounted SMB folder on my NAS.
