Bundled fswatch files are generated by `Scripts/prepare-fswatch.sh`.
Bundled rclone files are generated by `Scripts/prepare-rclone.sh`.

The generated executable and dylibs are intentionally ignored by Git.
